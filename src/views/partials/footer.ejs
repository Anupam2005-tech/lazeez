  </main>

  <% if (!user || user.role !== 'admin') { %>
    <!-- Floating checkout bar (appears after adding items) -->
    <style>
      #cart-toast:active { transform: scale(0.95); }
    </style>
    <a id="cart-toast" href="/checkout" style="display:none; position:fixed; bottom:20px; right:20px; z-index:9999; background:#60b246; border-radius:24px; padding:8px 16px; text-decoration:none; transition: all 0.2s ease; box-shadow: 0 4px 12px rgba(96,178,70,0.3);">
      <div style="display:flex; align-items:center; gap:6px;">
        <svg style="width:16px; height:16px; color:white;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
          <path stroke-linecap="round" stroke-linejoin="round" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z"/>
        </svg>
        <span id="cart-toast-count" style="display:none">0</span>
        <div style="font-size:13px; font-weight:700; color:white; letter-spacing: 0.5px;">Checkout</div>
        <svg style="width:14px; height:14px; color:white;" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="3">
          <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
        </svg>
      </div>
    </a>
  <% } %>

  <!-- Swiggy Style Dark Footer -->
  <footer class="bg-black text-white py-12 border-t border-gray-800 w-full">
  <div class="max-w-[1200px] mx-auto px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 md:gap-12 text-center md:text-left">
      
      <div class="flex flex-col items-center md:items-start">
        <a href="/" class="mb-4 inline-block">
          <img src="/img/logo-white.svg" alt="Lazeez" class="h-10 w-auto">
        </a>
        <p class="text-gray-400 text-sm leading-relaxed max-w-sm">
          Serving authentic flavors and fresh ingredients every day. Join us for a memorable dining experience or order your favorites straight to your door.
        </p>
        <p class="text-gray-500 text-xs mt-6">©  Lazeez Restaurant</p>
      </div>

      <div class="flex flex-col items-center md:items-start">
        <h4 class="text-lg font-bold mb-5 text-white">Visit Us</h4>
        <ul class="space-y-4 text-gray-400 text-sm">
          <li class="flex items-start justify-center md:justify-start gap-3">
            <span class="text-[#fc8019] shrink-0">📍</span>
            <span>Dhajanagar,near Netaji Subash Mahavidalaya,<br>Udaipur, Tripura 799114</span>
          </li>
          <li class="flex items-center justify-center md:justify-start gap-3">
            <span class="text-[#fc8019] shrink-0">📞</span>
            <a href="tel:+918974108608" class="hover:text-white transition-colors">+91 8974108608</a>
          </li>
          <li class="flex items-center justify-center md:justify-start gap-3">
            <span class="text-[#fc8019] shrink-0">✉️</span>
            <a href="mailto:bhowmikanupam33@gmail.com" class="hover:text-white transition-colors">hello@lazeez.com</a>
          </li>
        </ul>
      </div>

      <div class="flex flex-col items-center md:items-start">
        <h4 class="text-lg font-bold mb-5 text-white">Opening Hours</h4>
        <div class="w-full max-w-[250px] md:max-w-none">
          <ul class="space-y-2 text-gray-400 text-sm">
            <li class="flex justify-between gap-4">
              <span>Mon - Thu:</span>
              <span class="text-white">11:00 AM - 10:00 PM</span>
            </li>
            <li class="flex justify-between gap-4">
              <span>Fri - Sun:</span>
              <span class="text-white">11:00 AM - 11:30 PM</span>
            </li>
          </ul>
        </div>
        
        <div class="mt-8 flex gap-6">
          <a href="/terms" class="hover:text-white transition-colors text-xs uppercase tracking-widest font-semibold text-gray-500">Terms</a>
          <a href="/privacy" class="hover:text-white transition-colors text-xs uppercase tracking-widest font-semibold text-gray-500">Privacy</a>
          <a href="/admin/login" class="hover:text-gray-300 transition-colors text-xs uppercase tracking-widest font-semibold text-gray-700">Staff Portal</a>
        </div>
      </div>

    </div>
  </div>
</footer>

  <!-- Storefront Cart (must be loaded on all storefront pages) -->
  <script src="/js/storefront-cart.js"></script>

  <!-- Offer badge live sync -->
  <script>
  (function() {
    var badge = document.getElementById('offer-badge');
    if (!badge) return;
    function syncOfferBadge() {
      fetch('/offers/api/count', { headers: { Accept: 'application/json' } })
        .then(function(r) { return r.json(); })
        .then(function(data) {
          if (data.unseenCount > 0) {
            badge.textContent = data.unseenCount;
            badge.classList.remove('hidden');
          } else {
            badge.classList.add('hidden');
          }
        })
        .catch(function() {});
    }

    // Replace 15s Polling with Real-time SSE listener
    // Listen for 'offer:new' events pushed from server via EventSource
    if (window.RestoRealtime) {
       window.RestoRealtime.on('offer:new', function() {
         syncOfferBadge();
       });
    }
  })();
  </script>

<!-- Custom Toast Alert (replaces window.alert) -->
<style>
#resto-toast {
  position: fixed; top: 0; left: 0; right: 0; bottom: 0;
  z-index: 99999; display: none; align-items: center; justify-content: center;
  background: rgba(0,0,0,0.4); backdrop-filter: blur(4px);
}
#resto-toast.show { display: flex; }
#resto-toast-box {
  background: white; border-radius: 16px; padding: 24px; max-width: 340px; width: 90%;
  box-shadow: 0 20px 60px rgba(0,0,0,0.3); text-align: center;
  animation: resto-toast-in 0.2s ease-out;
}
@keyframes resto-toast-in { from { transform: scale(0.9); opacity: 0; } to { transform: scale(1); opacity: 1; } }
#resto-toast-msg { color: #3d4152; font-size: 14px; font-weight: 500; line-height: 1.5; margin-bottom: 16px; }
#resto-toast-ok {
  background: #fc8019; color: white; border: none; padding: 10px 32px;
  border-radius: 10px; font-size: 13px; font-weight: 700; cursor: pointer;
  text-transform: uppercase; letter-spacing: 0.5px;
}
#resto-toast-ok:hover { background: #e57317; }
</style>
<div id="resto-toast">
  <div id="resto-toast-box">
    <div id="resto-toast-msg"></div>
    <button id="resto-toast-ok">OK</button>
  </div>
</div>
<script>
(function() {
  var toast = document.getElementById('resto-toast');
  var msg = document.getElementById('resto-toast-msg');
  var ok = document.getElementById('resto-toast-ok');
  var callback = null;

  function showAlert(message, cb) {
    msg.textContent = message;
    toast.classList.add('show');
    callback = cb || null;
  }

  ok.addEventListener('click', function() {
    toast.classList.remove('show');
    if (callback) { callback(); callback = null; }
  });
  toast.addEventListener('click', function(e) {
    if (e.target === toast) {
      toast.classList.remove('show');
      if (callback) { callback(); callback = null; }
    }
  });

  window.RestoAlert = { show: showAlert };
  window.alert = function(m) { showAlert(m); };
})();
</script>



</body>
</html>
